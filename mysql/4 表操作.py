#__author__ = 'Winston'
#date: 2020/4/10

# 创建表
# 复制代码
# #语法：
# create table 表名(
# 字段名1 类型[(宽度) 约束条件],
# 字段名2 类型[(宽度) 约束条件],
# 字段名3 类型[(宽度) 约束条件]
# );
#
# #注意：
# 1. 在同一张表中，字段名是不能相同
# 2. 宽度和约束条件可选
# 3. 字段名和类型是必须的

# create table t1(
# id int,
# name varchar(50),
# sex enum('male','female'),
# age int(3)
# );

#Navicat12 快捷方式 ctrl+R 运行所有命令
# ctrl + R + shift 运行选择的命令
# f7 运行一行命令


